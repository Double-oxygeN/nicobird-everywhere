{
  "name": "nicobird-everywhere",
  "version": "1.1.0",
  "description": "Twitter client with niconico-like viewer.",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/electron .",
    "package": "./node_modules/.bin/electron-packager . --platform=darwin --arch=x64 --overwrite --prune --asar",
    "package-win": "./node_modules/.bin/electron-packager . --platform=win32 --arch=x64,ia32 --overwrite --prune --asar"
  },
  "author": "Double_oxygeN",
  "license": "Apache-2.0",
  "dependencies": {
    "electron": "^1.8.8",
    "electron-oauth-twitter": "^1.2.0",
    "twitter": "^1.7.1"
  },
  "devDependencies": {
    "electron-packager": "^13.1.1"
  }
}
